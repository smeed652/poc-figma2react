// Atom: RestaurantMenusText
import React from 'react';

export const RestaurantMenusText = ({ children }: { children: React.ReactNode }) => (
  <span className="text-gray-400 text-xs italic">{children}</span>
);
