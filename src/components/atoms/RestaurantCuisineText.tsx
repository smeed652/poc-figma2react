// Atom: RestaurantCuisineText
import React from 'react';

export const RestaurantCuisineText = ({ children }: { children: React.ReactNode }) => (
  <span className="text-gray-500 text-xs font-medium tracking-wide">{children}</span>
);
